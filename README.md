# README

* Ruby version -  3.4.1
* Setup environment with the uploaded Gemfile. 
* For development purpose, sqlite3 database was used locally.
* Execute rails db:migrate to run migrations.
* bundle exec rails s command should get your app running locally.
